@using GroupAssignment_3.Models;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model PeopleIndexVM[]
@{ 
    Layout = "_Layout";
    ViewBag.Title = "PersonBox";
}
<h3>People</h3>
<ul>
    @foreach (var person in Model)
    {
        <li>@Html.Partial("PersonBox", person)</li>

        @*if (person.ShowAsHighlighted)
        {
            <li style="color:green">@(person.Id).@person.Name - @person.Email</li>
                <li><a asp-action="Edit" asp-route-id="@person.Id">Edit</a></li>
        }
        else
        {
            <li>@(person.Id).@person.Name - @person.Email</li>
                <li><a asp-action="Edit" asp-route-id="@person.Id">Edit</a></li>
        }*@
    }
</ul>